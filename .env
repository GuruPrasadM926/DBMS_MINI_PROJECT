# MHMS backend environment variables
# Fill in the values (do NOT commit secrets to source control).

# MySQL connection
DB_HOST=localhost
DB_PORT=3306
DB_ADMIN_USER=mhms_admin
DB_NAME=MHMS
# MySQL connection
# Use 127.0.0.1 to force TCP (recommended for Workbench + app)
DB_HOST=127.0.0.1
DB_PORT=3306
DB_ADMIN_USER=root
# Fill your root password here (do NOT commit this file to source control)
DB_ADMIN_PASS=Guru@6364
DB_NAME=MHMS

# Backend server
PORT=4005

# Optional: TLS (true/false)
DB_SSL=false

# Example: SENTRY_DSN= (for error tracking)
